spring:
  application:
    name: orders-grpc
  jmx:
    enabled: false
  grpc:
    server:
      port: 9091
    client:
      channels:
        product-service:
          address: "static://localhost:9090"
          negotiation-type: plaintext